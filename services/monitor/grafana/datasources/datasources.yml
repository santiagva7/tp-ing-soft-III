apiVersion: 1

datasources:
  - name: Prometheus-Hot
    type: prometheus
    access: proxy
    url: http://prometheus-hot:9090
    isDefault: true
    jsonData:
      timeInterval: "15s"
      queryTimeout: "30s"
      httpMethod: "POST"
